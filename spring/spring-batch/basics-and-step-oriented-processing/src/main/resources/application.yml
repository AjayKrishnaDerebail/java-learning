spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/batch_schema
    username: ajaykdbadmin
    password: "Derebail@1234"
    sql:
      init:
        platform: mysql
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false